local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Leonid Master Galeri",
   LoadingTitle = "HD Görseller Hazırlanıyor...",
   ConfigurationSaving = { Enabled = false }
})

-- TÜM VERİLER VE ID'LER SENİN İÇİN EŞLEŞTİRİLDİ
local ResimListesi = {
    {
        Name = "GigaChad HD", 
        URL = "https://rentry.co/gigachad_data/raw", 
        ImageID = "rbxassetid://100767434959682"
    },
    {
        Name = "Psycho Vince (Original)", 
        URL = "https://rentry.co/vince_data/raw",
        ImageID = "rbxassetid://103478372698783" -- Senin verdiğin ID'lerden biriyle eşleşti
    },
    {
        Name = "Janet (Original)", 
        URL = "https://rentry.co/janet_data/raw",
        ImageID = "rbxassetid://78213107256210" -- Senin verdiğin ID'lerden biriyle eşleşti
    },
    {
        Name = "Özel Karakter 1", 
        URL = "https://rentry.co/data_resim1/raw",
        ImageID = "rbxassetid://86054612464800"
    },
    {
        Name = "Özel Karakter 2", 
        URL = "https://rentry.co/data_resim2/raw",
        ImageID = "rbxassetid://115307876136806"
    },
    {
        Name = "Özel Karakter 3", 
        URL = "https://rentry.co/data_resim3/raw",
        ImageID = "rbxassetid://76695795643588"
    }
}

local Tab = Window:CreateTab("🎨 Resim Seç", 4483362458)

for _, resim in pairs(ResimListesi) do
    local Section = Tab:CreateSection(resim.Name)
    
    -- Menüde resmi gösterir
    Tab:CreateLabel("Görsel:", resim.ImageID)
    
    Tab:CreateButton({
       Name = "🖌️ " .. resim.Name .. " Çizimini Başlat",
       Callback = function()
          Rayfield:Notify({
             Title = resim.Name, 
             Content = "Veri sunucudan alınıyor, lütfen bekleyin...", 
             Duration = 4,
             Image = resim.ImageID
          })
          
          local success, response = pcall(function() return game:HttpGet(resim.URL) end)
          if success and response ~= "" then
             local data = game:GetService("HttpService"):JSONDecode(response)
             local drawEvent = game:GetService("ReplicatedStorage"):FindFirstChild("Draw") or game:GetService("ReplicatedStorage"):FindFirstChild("DrawEvent")

             if drawEvent then
                for i, p in pairs(data) do
                   drawEvent:FireServer(Vector2.new(p.x, p.y), Color3.fromRGB(p.r, p.g, p.b))
                   -- Kaliteli çizim ve anticheat koruması için bekleme
                   if i % 30 == 0 then task.wait(0.01) end
                end
                Rayfield:Notify({Title = "Bitti!", Content = "Çizim başarıyla tamamlandı.", Duration = 3})
             else
                Rayfield:Notify({Title = "Sistem Hatası", Content = "Oyunun çizim sistemi bulunamadı!", Duration = 5})
             end
          else
             Rayfield:Notify({Title = "Hata", Content = "Resim verisi indirilemedi!", Duration = 5})
          end
       end,
    })
end

